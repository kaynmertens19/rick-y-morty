// objects to create the interface to interact with hte information of the episodes
export interface Episode {
  name: string;
  air_date: string;
  episode: string;
  id: string;
  characters: string[];
}